# 经管之星接口与数据库设计

## 1. 设计目标

基于当前 `demo.html` 中的演示数据，后端需要支撑三类能力：

- 台账数据管理：商业市场台账、PPL 明细台账、经营目标台账的内置 demo 数据初始化、查询和统计分析。
- 智能问数：GPT 只能选择受控接口并生成结构化参数，不能直接生成 SQL 查询数据库。
- 管理闭环：GPT 配置、接口管理、调用审计、AI 会话日志、回复校对反馈。

数据库统一采用 SQLite 单库，保持轻量部署。金额字段和比例字段在建表中使用 `NUMERIC`，由后端按业务精度校验；接口参数、返回字段和图表配置等结构化内容使用 JSON 字符串保存到 `TEXT` 字段。

## 2. 数据域拆分

| 数据域 | 表 | 说明 |
| --- | --- | --- |
| 维度主数据 | `dim_org_unit`、`dim_industry`、`dim_product_line`、`dim_product_model`、`dim_customer` | 经营单元、行业、产品线、产品型号、客户 |
| 台账原始数据 | `ledger_commercial_contract`、`ledger_commercial_monthly_metric`、`ledger_ppl_pipeline`、`ledger_goal_target` | 对应 demo 中商业市场台账、PPL 明细、整体目标 |
| 报表聚合数据 | `rpt_unit_achievement`、`rpt_product_line_summary`、`rpt_product_model_breakdown`、`rpt_industry_achievement`、`rpt_key_unit_product_analysis` | 对应经营单元达成、产品分析、行业达成、重点单元分析 |
| AI 问数接口 | `ai_query_interface`、`ai_query_interface_param`、`ai_query_interface_field`、`ai_query_call_audit` | 接口白名单、参数、返回字段、调用审计 |
| AI 会话反馈 | `qa_session`、`qa_message`、`qa_feedback` | 对话历史、消息、回复校对 |
| GPT 配置 | `llm_config` | 本机 demo 的 OpenAI GPT 配置 |

## 3. 核心表结构

本章节字段类型用于表达业务含义，最终建表类型以 SQLite 版本的 [db_schema.sql](db_schema.sql) 为准。

### 3.1 维度表

`dim_org_unit`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 主键 |
| unit_code | varchar(64) | 经营单元编码 |
| unit_name | varchar(128) | 经营单元名称，如北京代表处 |
| unit_type | varchar(32) | 代表处、办事处、系统部、渠道部 |
| parent_code | varchar(64) | 上级组织 |
| enabled | tinyint | 是否启用 |

`dim_industry`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| industry_code | varchar(64) | 行业编码 |
| industry_name | varchar(128) | 行业名称，如金融、数字政府、智能制造 |
| industry_category | varchar(128) | 行业大类，如政企、运营商、商业市场 |

`dim_product_line`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| line_code | varchar(64) | 产品线编码 |
| line_name | varchar(128) | 通用计算、智能计算、商业解决方案 |

`dim_product_model`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| model_code | varchar(64) | 产品型号编码 |
| model_name | varchar(128) | 920、920X、AT3500 G3、R5300 G5 等 |
| line_code | varchar(64) | 所属产品线 |

### 3.2 商业市场台账

`ledger_commercial_contract` 保存合同级主记录，覆盖 demo 中 `fields2026` 的主要字段。月度收入、回款、交付服务收入拆到 `ledger_commercial_monthly_metric`，避免 36 个横向月份字段长期膨胀。

关键字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 主键 |
| source_pk | varchar(64) | demo 字段“主键” |
| year | int | 年度 |
| seller_name | varchar(255) | 卖方 |
| dept_level1 | varchar(128) | 一级部门 |
| dept_level2 | varchar(128) | 业绩二级部门 |
| province | varchar(64) | 省份 |
| industry_category | varchar(128) | 行业大类 |
| industry_name | varchar(128) | 行业 |
| product_line_code | varchar(64) | 产品线编码 |
| product_model_name | varchar(128) | 产品型号 |
| sales_name | varchar(128) | 业绩销售 |
| marketing_name | varchar(128) | 行销 |
| org_unit_name | varchar(128) | 经营单元 |
| sales_admin_contact | varchar(128) | 销管/联系人 |
| sign_date | date | 签订日期 |
| contract_no | varchar(128) | 合同编号 |
| pre_sales_no | varchar(128) | 预销售单号 |
| opportunity_name | varchar(255) | 机会点名称 |
| opportunity_no | varchar(128) | 机会点编号 |
| contract_name | varchar(255) | 合同名称 |
| buyer_name | varchar(255) | 买方 |
| final_customer_name | varchar(255) | 最终客户 |
| customer_level | varchar(64) | 客户级别 |
| special_project | varchar(128) | 专项 |
| quantity | int | 台套数 |
| redundant_quantity | int | 销冗数量 |
| tax_rate | decimal(9,4) | 税率 |
| contract_total_amount | decimal(18,2) | 合同总价 |
| recognized_amount | decimal(18,2) | 已计收金额 |
| unrecognized_amount | decimal(18,2) | 未计收金额 |
| collected_amount | decimal(18,2) | 已回款金额 |
| uncollected_amount | decimal(18,2) | 未回款金额 |
| receivable_amount | decimal(18,2) | 应收金额 |
| order_amount | decimal(18,2) | 订货金额 |
| is_stat | tinyint | 是否统计 |
| is_order_stat | tinyint | 是否统计订货 |
| extra_json | json | 差异说明、互联网专项等扩展字段 |

`ledger_commercial_monthly_metric`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| contract_id | bigint | 关联商业台账 |
| metric_type | varchar(32) | `income`、`payment`、`delivery_income` |
| metric_month | tinyint | 1-12 |
| amount | decimal(18,2) | 金额 |

### 3.3 PPL 明细台账

`ledger_ppl_pipeline` 对应 demo 中 `fieldsPPL`。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 主键 |
| contract_no | varchar(128) | 合同编号 |
| pre_sales_or_opp_no | varchar(128) | 预销售/机会点编号 |
| industry_category | varchar(128) | 行业大类 |
| industry_name | varchar(128) | 行业小类 |
| org_unit_name | varchar(128) | 经营单元 |
| sales_name | varchar(128) | 销售 |
| project_name | varchar(255) | 项目名称 |
| final_customer_name | varchar(255) | 最终客户 |
| product_type | varchar(128) | 产品类型 |
| product_model_name | varchar(128) | 产品型号 |
| new_product_model_name | varchar(128) | 新产品型号 |
| quantity | int | 台套数 |
| amount | decimal(18,2) | 金额 |
| amount_without_tax | decimal(18,2) | 不含税金额 |
| landing_month | varchar(16) | 落地时间 |
| project_competition_risk | varchar(32) | 项目竞争风险 |
| contract_signing_risk | varchar(32) | 合同签订风险 |
| supply_delivery_risk | varchar(32) | 供应&交付风险 |
| risk_level | varchar(32) | 项目风险等级 |
| project_stage | varchar(64) | 项目阶段 |
| special_action | varchar(128) | 专项行动 |
| approval_level | varchar(64) | 立项级别 |
| listing_date | date | 挂网时间 |
| bidding_date | date | 招标时间 |
| contract_signing_date | date | 合同签订时间 |
| customer_delivery_date | date | 客户实际要货时间 |
| payment_date | date | 回款时间 |
| is_scheduled | tinyint | 是否排产 |
| signing_entity | varchar(128) | 签约主体 |
| progress_risk_desc | text | 项目进展及风险说明 |
| customer_category | varchar(128) | 客户分类 |

### 3.4 经营目标台账

`ledger_goal_target`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 主键 |
| org_unit_name | varchar(128) | 经营单元 |
| year | int | 年度 |
| commercial_target_amount | decimal(18,2) | 商业目标 |
| solution_target_amount | decimal(18,2) | 商解目标 |

### 3.5 报表聚合表

报表可由台账实时聚合，也可保留 `rpt_*` 聚合表。第一版 demo 使用内置数据初始化聚合表，不引入后台任务。

`rpt_unit_achievement`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| stat_date | date | 数据截止日期 |
| year | int | 年度 |
| org_unit_name | varchar(128) | 经营单元 |
| target_amount | decimal(18,2) | 不含税目标 |
| income_amount | decimal(18,2) | 已完成收入 |
| old_income_amount | decimal(18,2) | 同期收入 |
| yoy_rate | decimal(9,4) | 同比 |
| completion_rate | decimal(9,4) | 完成率 |
| guaranteed_amount | decimal(18,2) | 保底 |
| q2_forecast_amount | decimal(18,2) | Q2 预测完成 |
| forecast_completion_rate | decimal(9,4) | 预测完成率 |

`rpt_product_line_summary`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| year | int | 年度 |
| product_line_name | varchar(128) | 产品线 |
| amount | decimal(18,2) | 完成金额 |
| compare_amount | decimal(18,2) | 对比年度完成金额 |
| yoy_rate | decimal(9,4) | 同比 |

`rpt_product_model_breakdown`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| year | int | 年度 |
| product_line_name | varchar(128) | 产品线 |
| product_model_name | varchar(128) | 细分型号 |
| amount | decimal(18,2) | 完成金额 |
| compare_amount | decimal(18,2) | 对比金额 |

`rpt_industry_achievement`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| year | int | 年度 |
| industry_name | varchar(128) | 行业 |
| income_amount | decimal(18,2) | 收入额 |
| yoy_rate | decimal(9,4) | 同比 |
| guaranteed_amount | decimal(18,2) | 保底 |
| q2_forecast_amount | decimal(18,2) | Q2 总计 |
| q2_yoy_rate | decimal(9,4) | Q2 同比 |
| general_compute_amount | decimal(18,2) | 通用计算完成 |
| intelligent_compute_amount | decimal(18,2) | 智能计算完成 |
| business_solution_amount | decimal(18,2) | 商业解决方案完成 |

## 4. AI 问数接口管理表

`ai_query_interface`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 主键 |
| interface_code | varchar(128) | 接口编码，如 `biz.unitAchievement.query` |
| interface_name | varchar(128) | 接口名称 |
| group_name | varchar(64) | 分组 |
| method | varchar(16) | POST/GET |
| path | varchar(255) | 接口路径 |
| owner_dept | varchar(128) | 负责人/部门 |
| description | text | 业务说明 |
| security_policy | text | 安全策略 |
| cache_policy | varchar(128) | 缓存策略 |
| rate_limit_policy | varchar(128) | 限流策略 |
| status | varchar(16) | 启用、停用 |

`ai_query_interface_param`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| interface_code | varchar(128) | 接口编码 |
| param_name | varchar(128) | 参数名 |
| param_type | varchar(64) | int、string[]、object 等 |
| required | tinyint | 是否必填 |
| enum_json | json | 枚举值 |
| description | varchar(255) | 参数说明 |

`ai_query_interface_field`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| interface_code | varchar(128) | 接口编码 |
| field_name | varchar(128) | 返回字段 |
| field_label | varchar(128) | 中文名 |
| field_type | varchar(64) | string、decimal、percent 等 |
| unit | varchar(32) | 万元、%、条 |
| sensitive_level | varchar(32) | public、internal、sensitive |

`ai_query_call_audit`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| request_id | varchar(64) | 请求 ID |
| session_id | bigint | 会话 ID |
| client_name | varchar(128) | 调用端，本机 demo 默认 `demo` |
| interface_code | varchar(128) | 调用接口 |
| request_params | json | 请求参数 |
| response_row_count | int | 返回行数 |
| duration_ms | int | 耗时 |
| status | varchar(32) | success、failed、blocked |
| error_message | varchar(512) | 错误信息 |

`llm_config`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | int | 固定为 1 |
| provider | varchar(32) | 固定为 `openai` |
| base_url | varchar(255) | 默认 `https://api.openai.com/v1` |
| model_name | varchar(128) | GPT 模型名称 |
| api_key | text | API Key，本机 demo 保存，前端脱敏展示 |
| stream_enabled | tinyint | 是否启用流式输出 |
| temperature | decimal(4,2) | 生成温度 |
| max_output_tokens | int | 最大输出 token |

## 5. 接口设计

### 5.1 接口管理

#### 查询接口列表

`GET /api/query-interfaces`

请求参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| keyword | string | 搜索接口名称、编码、路径 |
| status | string | 启用、停用 |
| groupName | string | 接口分组 |

返回字段：

| 字段 | 说明 |
| --- | --- |
| interfaceCode | 接口编码 |
| interfaceName | 接口名称 |
| method | 请求方式 |
| path | 接口路径 |
| paramCount | 参数数量 |
| fields | 返回字段摘要 |
| status | 状态 |

#### 查询接口详情

`GET /api/query-interfaces/{interfaceCode}`

返回接口基础信息、参数列表、返回字段、安全策略、缓存策略、AI 调用示例。

#### 新增接口

`POST /api/query-interfaces`

请求体：

```json
{
  "interfaceCode": "biz.regionAchievement.query",
  "interfaceName": "区域收入达成查询",
  "groupName": "经营指标接口",
  "method": "POST",
  "path": "/api/ai-query/region-achievement",
  "description": "按区域查询收入、目标和完成率",
  "params": [
    {"name": "year", "type": "int", "required": true, "description": "年度"}
  ],
  "fields": [
    {"name": "regionName", "label": "区域", "type": "string"},
    {"name": "incomeAmount", "label": "收入额", "type": "decimal", "unit": "万元"}
  ],
  "securityPolicy": "只返回聚合指标，限制 pageSize 和可选字段",
  "cachePolicy": "5分钟"
}
```

#### 启用/停用接口

`PATCH /api/query-interfaces/{interfaceCode}/status`

```json
{"status": "停用"}
```

### 5.2 AI 问数执行接口

所有问数查询统一走 `/api/ai-query/{interfaceCode}`，后端根据接口编码路由到具体服务方法。

#### 首页经营概览

`POST /api/ai-query/biz.dashboard.summary`

支持参数：`year`、`dataAsOf`。

返回：目标金额、已完成收入、完成率、同比、风险项目数、产品线结构、行业 TopN。

#### 经营单元达成查询

`POST /api/ai-query/biz.unitAchievement.query`

请求：

```json
{
  "requestId": "uuid",
  "filters": {
    "year": 2026,
    "period": "YTD",
    "unitNames": ["北京代表处", "上海代表处"]
  },
  "metrics": ["targetAmount", "incomeAmount", "completionRate", "yoyRate"],
  "sort": [{"field": "incomeAmount", "direction": "desc"}],
  "page": 1,
  "pageSize": 100
}
```

响应：

```json
{
  "interfaceCode": "biz.unitAchievement.query",
  "dataAsOf": "2026-05-31",
  "columns": [
    {"field": "orgUnitName", "label": "经营单元", "type": "string"},
    {"field": "targetAmount", "label": "不含税目标", "type": "decimal", "unit": "万元"},
    {"field": "incomeAmount", "label": "已完成收入", "type": "decimal", "unit": "万元"},
    {"field": "completionRate", "label": "完成率", "type": "percent"}
  ],
  "rows": [],
  "summary": {
    "totalTargetAmount": 0,
    "totalIncomeAmount": 0
  }
}
```

#### 产品线完成分析

`POST /api/ai-query/biz.productLine.analysis`

支持参数：`year`、`compareYear`、`productLines`、`includeModelBreakdown`。

返回：产品线完成金额、对比金额、同比、细分型号金额。

#### 产品型号明细分析

`POST /api/ai-query/biz.productModel.breakdown`

支持参数：`year`、`compareYear`、`productLines`、`modelNames`、`pageSize`。

返回：产品线、型号、完成金额、对比金额、占比、排名。

#### 行业收入达成查询

`POST /api/ai-query/biz.industryAchievement.query`

支持参数：`year`、`industryNames`、`amountRange`、`metrics`。

返回：行业收入、同比、Q2 预测、通用计算/智能计算/商业解决方案收入和占比。

#### 重点单元产品分析

`POST /api/ai-query/biz.keyUnitProduct.analysis`

支持参数：`year`、`unitNames`、`productLines`、`includeBreakdown`。

返回：经营单元、产品线、完成金额、同比、型号拆分、行业拆分、分析文字。

#### 商机管道风险统计

`POST /api/ai-query/ledger.pplRisk.summary`

支持参数：`deadline`、`riskLevels`、`unitNames`、`projectStages`。

返回：风险等级、项目数、金额、风险类型、阶段、经营单元。

#### 商业市场台账聚合查询

`POST /api/ai-query/ledger.commercial.aggregate`

支持参数：`year`、`dimensions`、`filters`、`metrics`、`pageSize`。

约束：`dimensions` 和 `metrics` 必须在接口白名单中；禁止传表名、字段名和任意表达式。

#### 商业市场台账明细查询

`GET /api/ledgers/commercial`

支持参数：`year`、`keyword`、`unitNames`、`industryNames`、`productLines`、`page`、`pageSize`。

返回：商业市场台账分页明细。第一版只读，不提供新增、修改、删除。

#### PPL 明细台账查询

`GET /api/ledgers/ppl`

支持参数：`keyword`、`unitNames`、`riskLevels`、`projectStages`、`page`、`pageSize`。

返回：PPL 项目分页明细。第一版只读。

#### 经营目标查询

`GET /api/ledgers/goals`

支持参数：`year`、`unitNames`、`page`、`pageSize`。

返回：经营单元商业目标、解决方案目标。第一版只读。

### 5.3 AI 会话与反馈

#### 创建会话

`POST /api/qa/sessions`

#### 查询会话列表

`GET /api/qa/sessions?days=30&keyword=&userId=`

#### 发送消息

`POST /api/qa/sessions/{sessionId}/messages`

请求：

```json
{
  "content": "2026年各经营单元收入排名",
  "enabledInterfaces": ["biz.unitAchievement.query", "biz.industryAchievement.query"]
}
```

响应包含 AI 回复、使用的接口、接口参数、图表配置和追问建议。

#### 流式发送消息

`POST /api/qa/sessions/{sessionId}/messages/stream`

请求同发送消息接口，响应类型为 `text/event-stream`。

事件类型：

| 事件 | 说明 |
| --- | --- |
| `status` | 匹配接口、生成参数、调用接口、组织回答等阶段状态 |
| `interface_call` | 本次命中的接口编码和结构化参数 |
| `delta` | GPT 增量文本 |
| `chart` | 图表配置 |
| `suggestions` | 追问建议 |
| `done` | 完成 |
| `error` | 业务错误 |

#### 提交回复校对

`POST /api/qa/feedback`

```json
{
  "sessionId": 10001,
  "messageId": 20001,
  "question": "北京代表处今年达成情况",
  "answerSnippet": "北京代表处整体同比下降3%",
  "reason": "数据有误",
  "remark": "同比口径应按不含税收入计算"
}
```

#### 处理回复校对

`PATCH /api/qa/feedback/{feedbackId}`

```json
{
  "status": "已处理",
  "handlerRemark": "已核对，修正经营目标接口口径"
}
```

### 5.4 台账查询

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/ledgers/commercial` | GET | 查询商业市场台账 |
| `/api/ledgers/ppl` | GET | 查询 PPL 明细台账 |
| `/api/ledgers/goals` | GET | 查询经营目标 |

第一版 demo 使用内置数据，台账只查不改。聚合表由初始化逻辑一次性生成，保证智能问数接口读取到的统计口径一致。

### 5.5 GPT 配置

#### 查询 GPT 配置

`GET /api/settings/llm`

返回时 API Key 脱敏：

```json
{
  "provider": "openai",
  "baseUrl": "https://api.openai.com/v1",
  "modelName": "gpt-4o-mini",
  "apiKeyMasked": "sk-****abcd",
  "streamEnabled": true,
  "temperature": 0.2,
  "maxOutputTokens": 2048
}
```

#### 保存 GPT 配置

`PUT /api/settings/llm`

```json
{
  "baseUrl": "https://api.openai.com/v1",
  "apiKey": "sk-...",
  "modelName": "gpt-4o-mini",
  "streamEnabled": true,
  "temperature": 0.2,
  "maxOutputTokens": 2048
}
```

#### 测试 GPT 连接

`POST /api/settings/llm/test`

用于保存前验证 Base URL、API Key 和模型名称是否可用。

## 6. AI 问数安全规则

- AI 输出的是 `{interfaceCode, filters, metrics, dimensions, sort, limit}`，不是 SQL。
- 后端必须校验接口是否启用、参数是否符合契约。
- 后端必须限制 `pageSize` 和最大返回行数。
- 所有明细类接口默认不返回客户敏感字段；如 demo 展示需要返回，必须按固定字段规则脱敏。
- 所有接口调用写入 `ai_query_call_audit`。
- 数据库错误不能原样返回给前端和 AI，只返回业务错误码。

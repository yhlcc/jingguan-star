# 经管之星技术架构建议（历史版本）

> 本文记录初版 Demo 的架构建议，不再代表当前实现。生产级重构方案请以
> [`docs/production-architecture.md`](docs/production-architecture.md) 为准，实施步骤请以
> [`docs/implementation-plan.md`](docs/implementation-plan.md) 为准。

## 1. 总体选择

本项目统一采用轻量单体架构：

- 前端：React。
- 后端：Python。
- 数据库：SQLite 单库单文件。
- 智能问数：GPT 只生成接口选择和结构化参数，不直接生成 SQL。
- 数据来源：内置 demo 数据初始化到 SQLite，第一版只查不改。

推荐形态：

```text
React 前端
  -> Python API 服务
    -> GPT 流式问答/接口白名单/参数校验/审计
      -> SQLite
```

## 2. 为什么统一 SQLite

当前阶段不考虑数据迁移、文件导入、后台任务和测试体系，业务数据也来自 demo 原型抽象出的台账、聚合报表和问数接口管理，因此没有必要拆成两个数据库。

统一 SQLite 的好处：

- 部署轻：一个数据库文件即可运行。
- 运维少：不需要单独安装数据库服务。
- 数据简单：内置 demo 数据、接口契约、问数审计和会话反馈都在同一个库文件。
- 口径一致：业务台账、接口契约、问数审计、会话反馈都在同一个库里，减少同步问题。

不拆库的边界：

- 不把 AI 会话、接口契约、业务台账拆到不同数据库。
- 不引入分析型数据库。
- 不引入导入任务表和后台刷新任务。

## 3. 前端三方件建议

| 类型 | 建议 | 用途 |
| --- | --- | --- |
| 构建工具 | Vite | React 项目开发和打包 |
| 语言 | TypeScript | 降低接口字段和页面状态错误 |
| UI 组件 | Ant Design | 表格、表单、弹窗、菜单、筛选器 |
| 图表 | ECharts | 经营达成、同比、产品结构等图表 |
| 路由 | React Router | 页面路由 |
| 请求 | Axios | 调用后端 API |
| 服务端状态 | TanStack Query | 列表查询、缓存、刷新 |

前端模块建议：

- 首页看板。
- 智能问数。
- 台账查询。
- 问数接口管理。
- 问数审计。
- 回复校对。
- GPT 模型配置。

## 4. 后端三方件建议

| 类型 | 建议 | 用途 |
| --- | --- | --- |
| Web 框架 | FastAPI | 提供 REST API，自动生成 OpenAPI |
| ASGI 服务 | Uvicorn | 本地和轻量部署运行 |
| 数据校验 | Pydantic | 请求参数、接口契约和返回结构校验 |
| 数据库 | sqlite3 | Python 标准库，直接连接 SQLite |
| GPT 调用 | openai | 调用 OpenAI GPT，支持流式输出 |

默认不引入 SQLAlchemy，保持依赖更轻。后端可使用 `sqlite3` 封装 repository 层，所有 SQL 由后端代码维护，AI 不能拼接或提交 SQL。

如果后续 CRUD 复杂度明显上升，再考虑引入 SQLAlchemy Core 或 ORM。

## 5. 后端分层

```text
api/
  controllers     接收 HTTP 请求
  schemas         Pydantic 请求/响应模型
  services        业务逻辑、口径计算、GPT 编排
  repositories    SQLite 访问
  interface_gate  问数接口白名单、参数校验、字段过滤
  seed            内置 demo 数据初始化
  settings        GPT 配置读写
```

核心规则：

- 前端“数据源选择”改为“接口选择”。
- 接口管理模块维护接口编码、路径、参数、返回字段、安全说明和状态。
- AI 问数请求只包含 `interfaceCode`、`filters`、`metrics`、`dimensions`、`sort`、`limit`。
- 后端根据 `interfaceCode` 找到受控接口并执行固定查询逻辑。
- 每次接口调用写入 `ai_query_call_audit`。
- 数据库错误不直接返回给前端或 AI，只返回业务错误码和可读提示。
- 发送问数消息时使用 SSE 或 Fetch ReadableStream 做流式输出。

## 6. SQLite 使用约定

- 数据库文件建议：`data/jingguan_star.db`。
- 建表脚本：`db_schema.sql`。
- 初始化数据由后端启动时检测并写入，来源按 demo 原型自行生成。
- 日期时间统一存储为 `TEXT`，格式由后端约束为 `YYYY-MM-DD` 或 ISO 时间。
- 金额、比例使用 `NUMERIC`，由 Pydantic 和 service 层控制精度。
- JSON 内容存入 `TEXT`，读写时由后端序列化和反序列化。
- `updated_at` 由应用在更新数据时显式赋值，不依赖数据库触发器。

## 7. 本机运行方式

推荐本地启动方式：

```text
frontend: npm run dev
backend:  uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
sqlite:   data/jingguan_star.db
```

前端通过 `VITE_API_BASE_URL=http://127.0.0.1:8000` 调用后端。

## 8. GPT 配置与流式输出

- 系统管理 / 模型配置页面提供 OpenAI GPT 配置。
- 配置项保存到 SQLite 的 `llm_config` 表。
- API Key 只在本机 demo 中保存使用，前端展示时必须脱敏。
- 问数消息接口支持流式输出，推荐后端返回 `text/event-stream`。
- 所有模型调用（意图识别、Skill 匹配、查询规划、结构化回答、自然语言回答）都以 `stream=true` 调用 OpenAI / DeepSeek Chat Completions，按 SSE token 解析；
  自然语言回答把 token 增量通过事件推给前端，结构化 JSON 节点流式接收后解析返回，`stream_enabled=false` 时退化为一次性返回。
  流式过程中先输出“正在匹配 Skill、正在调用接口、正在生成回答”等状态，再输出模型 token 流。

## 9. 当前不纳入范围

- 数据迁移。
- 文件导入。
- 后台任务。
- 自动化测试体系。
- 拆分多个数据库。
- 权限管理。
- 台账新增、修改、删除。

# 经管之星需求说明

技术架构、接口契约和数据库表结构详见：[技术架构.md](技术架构.md)、[接口与数据库设计.md](接口与数据库设计.md) 与 [db_schema.sql](db_schema.sql)。

## 1. 当前调整背景

智能问数原方案中，AI 根据用户问题直接生成 SQL 访问数据库，存在较高安全风险：

- AI 可能生成越权查询、全表扫描、敏感字段查询或破坏性语句。
- 查询口径不可控，容易绕过业务指标定义。
- 缺少统一鉴权、限流、脱敏、审计和缓存机制。
- SQL 暴露后，数据库结构和字段含义容易泄漏。

因此调整为：**AI 不直接访问数据库，也不生成可执行查询语句；AI 只选择已启用接口并生成结构化请求参数，由后端接口完成数据库查询。**

## 1.1 第一版 demo 落地边界

- 数据使用内置 demo 数据，由系统初始化生成到 SQLite。
- 台账第一版只查询，不提供新增、修改、删除和文件导入。
- 当前不做权限管理，不建设用户、角色和数据范围管理模块。
- demo 原型中提到的数据域都需要有对应查询接口，形成“页面展示、接口管理、AI 问数、调用审计、回复校对”的闭环。
- AI 接入真实 GPT 大模型，由后端调用模型服务。
- 系统管理中提供 GPT 配置界面，可设置 Base URL、API Key、模型名称、流式输出开关等。
- 智能问数需要支持流式输出。
- 部署方式为本机运行。

## 2. 新架构方案

### 2.1 调用链路

用户提问 -> GPT 意图识别 -> 接口选择 -> 参数生成 -> 后端接口校验 -> 参数校验 -> 预置查询模板执行 -> 数据脱敏/聚合 -> GPT 流式组织回答和图表

### 2.2 核心原则

- 接口白名单：AI 只能调用接口管理模块中启用的接口。
- 参数白名单：AI 只能传接口定义中允许的参数、枚举值、指标和维度。
- 查询受控：真实查询逻辑在后端接口内实现，使用预置查询模板或服务层查询方法。
- 安全前置：当前 demo 不做权限管理，但接口仍需做启用状态、参数白名单、字段白名单和返回行数限制。
- 结果收敛：默认返回聚合数据，明细数据必须单独授权、分页、脱敏。
- 全链路审计：记录提问、接口编码、请求参数、调用人、响应规模、耗时、结果状态。

## 3. 问数接口形式

统一采用 HTTP JSON API。推荐基础形式：

```http
POST /api/ai-query/{interfaceCode}
Content-Type: application/json
```

请求体建议：

```json
{
  "requestId": "uuid",
  "intent": "经营单元达成查询",
  "filters": {
    "year": 2026,
    "unitCodes": ["BJ", "SH"]
  },
  "dimensions": ["unitName"],
  "metrics": ["targetAmount", "incomeAmount", "completionRate"],
  "sort": [{"field": "incomeAmount", "direction": "desc"}],
  "page": 1,
  "pageSize": 100
}
```

响应体建议：

```json
{
  "requestId": "uuid",
  "interfaceCode": "biz.unitAchievement.query",
  "dataAsOf": "2026-05-31",
  "columns": [
    {"field": "unitName", "label": "经营单元", "type": "string"},
    {"field": "incomeAmount", "label": "收入额", "type": "decimal", "unit": "万元"}
  ],
  "rows": [],
  "summary": {},
  "warnings": [],
  "trace": {
    "durationMs": 126,
    "rowCount": 20,
    "dataScope": "demo内置数据"
  }
}
```

## 4. 第一版接口清单

| 接口名称 | 接口编码 | 路径 | 主要参数 | 返回字段 |
| --- | --- | --- | --- | --- |
| 首页经营概览 | biz.dashboard.summary | /api/ai-query/dashboard-summary | year、dataAsOf | totalTargetAmount、totalIncomeAmount、completionRate、yoyRate、riskProjectCount |
| 经营单元达成查询 | biz.unitAchievement.query | /api/ai-query/unit-achievement | year、period、unitCodes、metrics | unitName、targetAmount、incomeAmount、yoyRate、completionRate、productLineMix |
| 产品线完成分析 | biz.productLine.analysis | /api/ai-query/product-line-analysis | year、productLines、compareYear | productLine、amount、compareAmount、yoyRate、modelBreakdown |
| 产品型号明细分析 | biz.productModel.breakdown | /api/ai-query/product-model-breakdown | year、productLines、modelNames、compareYear | productLine、modelName、amount、compareAmount、shareRate |
| 行业收入达成查询 | biz.industryAchievement.query | /api/ai-query/industry-achievement | year、industryCodes、amountRange | industryName、incomeAmount、yoyRate、forecastAmount、productLineRatio |
| 重点单元产品分析 | biz.keyUnitProduct.analysis | /api/ai-query/key-unit-product-analysis | year、unitNames、productLines | unitName、productLine、amount、yoyRate、modelBreakdown、industryBreakdown、analysisText |
| 商机管道风险统计 | ledger.pplRisk.summary | /api/ai-query/ppl-risk-summary | deadline、riskLevel、unitCodes | riskLevel、projectCount、amount、riskType、stage、unitName |
| 商业市场台账聚合查询 | ledger.commercial.aggregate | /api/ai-query/commercial-aggregate | year、dimensions、filters、metrics | dimensionValues、incomeAmount、orderAmount、receivedAmount、unreceivedAmount、recordCount |
| 商业市场台账明细查询 | ledger.commercial.detail | /api/ledgers/commercial | year、keyword、unitNames、industryNames、productLines、page、pageSize | contractNo、contractName、buyerName、incomeAmount、orderAmount、collectedAmount |
| PPL 明细台账查询 | ledger.ppl.detail | /api/ledgers/ppl | keyword、unitNames、riskLevels、projectStages、page、pageSize | projectName、unitName、riskLevel、amount、stage、riskDesc |
| 经营目标查询 | ledger.goal.query | /api/ledgers/goals | year、unitNames、page、pageSize | unitName、commercialTargetAmount、solutionTargetAmount |
| 问数反馈提交 | qa.feedback.create | /api/qa/feedback | sessionId、question、answerSnippet、reason | feedbackId、status、createdAt |

## 5. 接口选择调整

原“选择数据源”调整为“选择问数接口”。

用户在智能问数中选择的是可调用接口，而不是数据库、表或文件。选择弹窗需要展示：

- 接口分组
- 接口名称
- 接口编码
- 请求方式和路径
- 业务说明
- 请求参数
- 返回字段
- 安全策略
- 状态

AI 处理问题时，应展示“匹配问数接口、生成接口参数、调用受控查询接口、解析结果”的过程，不展示 SQL。

## 6. 接口管理模块

新增菜单：系统管理 / 接口管理。

### 6.1 列表字段

- 接口名称
- 接口编码
- 请求方式
- 接口路径
- 负责人
- 请求参数数量
- 返回字段
- 状态：启用、停用
- 操作：详情、启用/停用、新增

### 6.2 详情字段

- 接口编码
- 接口地址
- 业务说明
- 请求参数定义
- 返回字段定义
- 安全策略
- 缓存策略
- 限流策略
- 审计策略
- AI 调用示例

### 6.3 新增/编辑字段

- 接口名称
- 接口编码
- 请求方式
- 接口路径
- 所属分组
- 负责人
- 请求参数
- 返回字段
- 业务说明
- 安全策略
- 是否启用

## 6.4 GPT 配置模块

新增菜单：系统管理 / 模型配置。

字段：

- Provider：支持 OpenAI、DeepSeek V4。
- Base URL：OpenAI 默认 `https://api.openai.com/v1`；DeepSeek V4 默认 `https://api.deepseek.com`。
- API Key：密文输入，后端保存，本机 demo 使用。
- Model Name：可配置模型名称，DeepSeek V4 支持 `deepseek-v4-pro`、`deepseek-v4-flash`。
- Stream：是否启用流式输出，默认启用。
- Temperature：默认 0.2。
- Max Output Tokens：默认 2048。

发送消息接口需要支持 `text/event-stream`，前端按增量内容实时渲染回答。

## 7. 安全约束

- 禁止 AI 生成可执行 SQL。
- 禁止 AI 访问未登记接口。
- 禁止接口透传任意 SQL、表名、字段名。
- 接口必须限制 pageSize 和最大返回行数。
- 敏感字段默认不返回；确需返回时必须脱敏并单独授权。
- 每次接口调用必须写审计日志。
- 接口异常时只返回业务错误码，不暴露数据库错误和表结构。
